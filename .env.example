# Backend environment variables
# Replace with your own connection string
MONGO_URI=mongodb+srv://<username>:<password>@<cluster-host>/NimbleHomeDB?retryWrites=true&w=majority
PORT=5000
# Uncomment if you use JWT-based routes
# JWT_SECRET=yourSuperSecretKey 